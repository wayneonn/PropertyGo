import React from 'react';
import {Button, SafeAreaView, Text, View} from 'react-native';

const FlaggedComments = ({navigation}) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Welcome to the FlaggedComments!</Text>
            </View>

        </SafeAreaView>
    );
};

export default FlaggedComments;