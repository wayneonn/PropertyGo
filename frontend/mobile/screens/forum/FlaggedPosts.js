import React from 'react';
import {Button, SafeAreaView, Text, View} from 'react-native';

const FlaggedPosts = ({navigation}) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Welcome to the FlaggedPosts!</Text>
            </View>

        </SafeAreaView>
    );
};

export default FlaggedPosts;